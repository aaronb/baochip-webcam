Vendored from https://github.com/betrusted-io/usb-device.git, branch `main`, commit 7ab6ba8
(upstream usb-device 0.2.8 with the betrusted `baochip-debug` feature). Local change: a
`control-buffer-512` feature (see `src/control_pipe.rs`). The workspace `Cargo.toml` patches
both the crates-io name and the git URL to this directory.
